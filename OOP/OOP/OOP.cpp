#include <iostream>
#include "Number.h"
#include "User.h"
using namespace std;

/*
Name,
Surname,
Birthday, (year, mm, dd)
Country,
Username,
Password
*/

int main()
{
	User user;
	user.reg();
	
   /*
   Number number;
   number.setValue(5);

   Number number2;
   number2.setValue(10);

   Number number3;
   number3.setValue(7);

   cout << number.sum(number2)
				 .substract(number3).getValue() << "\n";
  */
}