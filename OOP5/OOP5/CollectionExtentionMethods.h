#pragma once
#include "List.h"
#include "Stack.h"
#include "Queue.h"

template<typename T>
class CollectionExtentionMethods
{
    // stack LIFO
    // queue FIFO

public:
    /*
    static Stack<T> ToStack(const List<T> & input)
    {
        Stack<T> output;
        int size = input.length();
        for (int i = 0;i < size;i++)
        {
            output.add(input.get(i));
        }

        return output;
    }
        static Stack<T> ToStack(const Queue<T> & input)
    {
        Stack<T> output;
        int size = input.length();
        for (int i = size-1;i >= 0;i++)
        {
            output.add(input.get(i));
        }

        return output;
    }

    static Queue<T> ToQueue(const List<T>& input)
    {
        Queue<T> output;
        int size = input.length();
        for (int i = 0;i < size;i++)
        {
            output.add(input.get(i));
        }

        return output;
    }

    static Queue<T> ToQueue(const Stack<T> & input)
    {
        Queue<T> output;
        int size = input.length();
        for (int i = 0;i < size;i++)
        {
            output.add(input.get(i));
        }

        return output;
    }

    List<T> ToList(const Stack<T>& input)
    {
        List<T> output;
        int size = input.length();
        for (int i = 0;i < size;i++)
        {
            output.add(input.get(i));
        }

        return output;
    }

    List<T> ToList(const Queue<T>& input)
    {
        List<T> output;
        int size = input.length();
        for (int i = size - 1;i >= 0;i++)
        {
            output.add(input.get(i));
        }

        return output;
    }
    */
};

